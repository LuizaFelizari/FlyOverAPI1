spring.application.name=flyover
# Define cores no terminal
spring.output.ansi.enabled=always
# Define path raiz do endpoint
# http://localhost:8080/flyover/algumacoisa
server.servlet.context-path=/flyover

